/* This is the default LXCManager configuration file
 *
 * Update it carefully */
{
	/* plugin::Connection options:
	 * Specify it there implies that:
	 *  - this plugin is required
	 *  - it will be loaded and initialized and stared
	 *  - these options will be allowed to this plugin */
	"plugin::Communication" : {
		"port" : 4242
	}
}
